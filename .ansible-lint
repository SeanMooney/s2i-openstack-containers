---
profile: production
exclude_paths:
  - .tmp/
mock_modules:
  - zuul_return
mock_roles:
  - collect-container-logs
  - ensure-output-dirs
  - ensure-podman
  - ensure-python
  - run-buildset-registry
  - use-buildset-registry
